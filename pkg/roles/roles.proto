syntax = "proto3";

package roles;

option go_package = "github.com/dchenk/mazewire/pkg/roles";

// A Role is a role a user can have on a site.
enum Role {
	NONE = 0;

	SUBSCRIBER = 1;

	AUTHOR = 3;

	EDITOR = 5;

	ADMIN = 7;

	OWNER = 9;

	SUPER = 11;
}
